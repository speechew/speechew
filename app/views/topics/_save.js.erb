$("ul.errors").html("")
<% if @topic.errors.any? %>
  <% @topic.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>  
  $('#topics-table').DataTable().ajax.reload();
  $("#global-modal").modal("hide")
<% end %>