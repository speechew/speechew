<div class="d-flex flex-column flex-lg-row ">
  <div class="mr-auto">
    <h3>Pages</h3> 
  </div>
  <p><% if can? :create, Page %><%= link_to "New Page", new_page_path, remote: true, class: "btn btn-xs btn-primary" %><% end %></p>
</div>

<div class="col-md-12">
  <div class="panel panel-inverse">
    <div class="panel-body">    
      <table id="pages-table" data-source="<%= pages_path(format: :json) %>" class="table table-striped table-bordered dataTable no-footer" width="100%">
        <thead>
          <tr>
            <th>Code</th>
            <th>Title</th>
		    <th>Operations</th>
          </tr>
        </thead>
        <tbody>
        </tbody>
      </table>	
    </div>
  </div>
</div>