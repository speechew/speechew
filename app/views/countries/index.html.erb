<div class="d-flex flex-column flex-lg-row ">
  <div class="mr-auto">
    <h3>Countries</h3> 
  </div>
  <p><% if can? :create, Country %><%= link_to "New Country", new_country_path, remote: true, class: "btn btn-xs btn-primary" %><% end %></p>
</div>

<div class="col-md-12">
  <div class="panel panel-inverse">
    <div class="panel-body">    
      <table id="countries-table" data-source="<%= countries_path(format: :json) %>" class="table table-striped table-bordered dataTable no-footer" width="100%">
        <thead>
          <tr>
            <th>Name</th>
            <th>Country code</th>
            <th>Continent name</th>
		    <th>Operations</th>
          </tr>
        </thead>
        <tbody>
        </tbody>
      </table>	
    </div>
  </div>
</div>